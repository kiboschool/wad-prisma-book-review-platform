{
  "name": "prisma-book-review-platform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "seed": "node ./prisma/seed.js",
    "test:prepare": "npx prisma migrate reset --force && npx prisma migrate dev --name init  && npm run seed",
    "test": "npm run test:prepare && jest",
    "grade": "npm run test:prepare && jest --bail=0 --testResultsProcessor jest-autograding-reporter"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@prisma/client": "^5.3.0",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "jest-autograding-reporter": "^0.1.6",
    "jest-environment-jsdom": "^29.5.0",
    "prisma": "^5.3.0",
    "supertest": "^6.3.3"
  }
}
